/* package main

import "fmt"

func main() {
	// 匿名函数
	// 自己调用自己
	r := func(a int, b int) int {
		if a > b {
			return a
		}
		return b
	}(9, 19)
	fmt.Printf("r: %v\n", r)
	/* 	max := func(a int, b int) int {
	   		if a > b {
	   			return a
	   		}
	   		return b
	   	}
	   	r := max(10, 100)
	   	fmt.Printf("r: %v\n", r) */
}
 */